<h1>House Ownership Change Detected</h1>

<p>A house in the neighborhood has changed ownership:</p>

<h2><%= @house.street_number %> <%= @house.street_name %></h2>

<% if @changes[:residents_removed].any? %>
<h3>Previous Residents (moved out):</h3>
<ul>
  <% @changes[:residents_removed].each do |resident| %>
    <li><%= resident.official_name || resident.display_name || 'Unknown' %></li>
  <% end %>
</ul>
<% end %>

<% if @changes[:residents_added].any? %>
<h3>New Residents:</h3>
<ul>
  <% @changes[:residents_added].each do |resident| %>
    <li><%= resident.official_name || resident.display_name || 'Unknown' %></li>
  <% end %>
</ul>
<% end %>

<h3>Verify on PCPAO</h3>
<p>Click the link below to verify this change on the Pinellas County Property Appraiser's Office website:</p>

<p><%= link_to "Search PCPAO Property Records", @pcpao_url, style: "background-color: #4F46E5; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; display: inline-block;", target: "_blank" %></p>

<p><strong>Search for:</strong> <%= @house.street_number %> <%= @house.street_name %></p>

<hr style="margin: 24px 0; border: none; border-top: 1px solid #e5e7eb;">

<p style="font-size: 12px; color: #6b7280;">
This notification was automatically generated by the Lake Pasadena Estates Neighborhood Directory system.
</p>